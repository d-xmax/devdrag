{
  "name": "bootstrap",
  "version": "1.0.0",
  "description": "my web",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "gsap": "^3.12.5",
    "parcel": "^2.12.0",
    "sass": "^1.77.8",
    "split-text-js": "^1.0.3"
  },
  "scripts": {
    "start": "rm -rf dist-dev .parcel-cache && parcel --dist-dir dist-dev index.html --port 8081",
    "build": "rm -rf dist .parcel-cache && parcel build  index.html"
  },
  "author": "dx",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0"
  }
}
